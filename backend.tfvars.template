gcp_credentials = "${GCLOUD_SERVICE_KEY}"
gcp_project_id = "${GOOGLE_PROJECT_ID}"
gcp_region = "${GOOGLE_PROJECT_REGION}"

bucket = "${GCP_BACKEND_BUCKET}"
prefix = "${PREFIX}"
credentials = "${GCLOUD_SERVICE_KEY}"